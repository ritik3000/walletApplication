# Copy this file to .env and fill the values. Do NOT commit your real .env.
# Example DATABASE_URL (Postgres/Neon). Replace placeholders with your values.
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"

DATABASE_URL="postgresql://<db_user>:<db_password>@<host>:5432/<db_name>"

# JWT secret key used to sign tokens
SECRET_KEY="your_jwt_secret_here"

# Optional: set NODE_ENV=development to enable additional logging
# NODE_ENV=development
